// Solución de Antonio González Cruz

ds=(ds-((dia%7)-1))%7;  // asignamos a ds el dia 1 de cada mes en función del
			// dia solicitado...

for(i=0;i<ds;i++)       // Debe funcionar para todos los casos
  printf("   ");        // crea los espacios vacios

for(i=1;i<=mesd;i++){
  printf("%3d",i);      // imprime el numero de dia usando 3 espacios
  if(((i+ds)%7)==0)
   printf("\n");        // salto de linea si llegamos a domingo
}